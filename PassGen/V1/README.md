# 🔑 PassGen – Random Password Generator

A simple Python-based password generator that creates secure random passwords with customizable options for digits and special characters.  
The script uses Python’s built-in `random` and `string` modules, along with ANSI color codes for styled console output.

---

## ✨ Features
- Generate random passwords of any length.
- Optionally include:
  - ✅ Digits (0–9)
  - ✅ Special characters (`!@#$%^&*...`)
- Graceful error handling with default values.
- Color-coded console messages using ANSI escape sequences.

---

## 📂 Project Structure